
// initial state
const state = {
  // login:          'user/login' ,                //登录
  category:       'category/list',              //获取所有课程分类
  getChannelStat:'getChannelStat',

  getPerson:'getPerson.php'
}

// getters
const getters = {
}

// actions
const actions = {
 
}

// mutations
const mutations = {
  
}

// for(let key in state){
//     state[key] = baseUrl + state[key]
// }
export { state }

export default {
  state,
  getters,
  actions,
  mutations
}